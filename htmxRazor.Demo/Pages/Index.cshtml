@page
@model IndexModel
@{
    Layout = "_DocsLayout";
    ViewData["Title"] = "htmxRazor";
    ViewData["HideSidebar"] = true;
}

<!-- Hero -->
<section class="docs-hero">
    <h1 class="docs-hero__title">htmxRazor</h1>
    <p class="docs-hero__subtitle">
        Server-rendered UI components for ASP.NET Core with first-class htmx integration.
    </p>
    <div class="docs-hero__actions">
        <rhx-button rhx-variant="brand" rhx-size="large" rhx-href="/Docs/GettingStarted">
            Get Started
        </rhx-button>
        <rhx-button rhx-variant="neutral" rhx-appearance="outlined" rhx-size="large"
                    rhx-href="https://github.com/cwoodruff/htmxRazor" rhx-target="_blank">
            View on GitHub
        </rhx-button>
    </div>
</section>

<rhx-divider />

<!-- Features -->
<section class="docs-features">
    <div class="docs-feature-card">
        <rhx-icon rhx-name="grid" rhx-size="32" />
        <h3>Tag Helper Components</h3>
        <p>90+ server-rendered components across 11 categories. Buttons, forms, date/time pickers, data tables, kanban boards, navigation, overlays, feedback, and more.</p>
    </div>
    <div class="docs-feature-card">
        <rhx-icon rhx-name="refresh" rhx-size="32" />
        <h3>htmx Integration</h3>
        <p>Every component supports hx-get, hx-post, hx-target, hx-swap, and all htmx attributes natively.</p>
    </div>
    <div class="docs-feature-card">
        <rhx-icon rhx-name="palette" rhx-size="32" />
        <h3>Design Tokens</h3>
        <p>Light and dark themes via CSS custom properties. Toggle with a single attribute or JavaScript call.</p>
    </div>
</section>

<rhx-divider />

<!-- What's New in v2.1 -->
<section style="max-width: 900px; margin: 0 auto; padding: var(--rhx-space-2xl) var(--rhx-space-xl);">
    <h2 style="text-align: center; font-size: var(--rhx-font-size-2xl); font-weight: var(--rhx-font-weight-bold); margin-bottom: var(--rhx-space-lg); color: var(--rhx-color-text);">
        What's New in v2.1 — Advanced Inputs
    </h2>
    <p style="text-align: center; color: var(--rhx-color-text-muted); margin-bottom: var(--rhx-space-xl);">
        A date/time picker family, a radial category picker, and Playwright E2E back in CI.
    </p>

    <div class="docs-features">
        <div class="docs-feature-card">
            <h3>Radial Select</h3>
            <p>A rectangular trigger opens a circular pie of color- and icon-labeled category wedges. Picking a wedge repopulates a dropdown via an htmx cascade and auto-selects the first option. Fully keyboard-accessible as a menu.</p>
            <rhx-button rhx-variant="brand" rhx-appearance="plain" rhx-size="small" rhx-href="/Docs/Components/RadialSelect">View docs</rhx-button>
        </div>
        <div class="docs-feature-card">
            <h3>Date Picker</h3>
            <p>Text input + popup calendar with server-rendered month navigation. Commits a hidden ISO <code>yyyy-MM-dd</code> value for model binding, with min/max bounds, configurable week start, and APG grid keyboard accessibility.</p>
            <rhx-button rhx-variant="brand" rhx-appearance="plain" rhx-size="small" rhx-href="/Docs/Components/DatePicker">View docs</rhx-button>
        </div>
        <div class="docs-feature-card">
            <h3>Time Picker</h3>
            <p>Text input + popup list of selectable times stepping by a configurable minute interval. Commits a hidden ISO <code>HH:mm</code> value with 12- or 24-hour display, and listbox keyboard navigation with type-ahead.</p>
            <rhx-button rhx-variant="brand" rhx-appearance="plain" rhx-size="small" rhx-href="/Docs/Components/TimePicker">View docs</rhx-button>
        </div>
        <div class="docs-feature-card">
            <h3>Date &amp; Time Picker</h3>
            <p>A single <code>DateTime</code> control pairing the calendar with a time list. Commits a hidden ISO <code>yyyy-MM-ddTHH:mm</code> value once both date and time are set. Reuses the Date Picker calendar and Time Picker list.</p>
            <rhx-button rhx-variant="brand" rhx-appearance="plain" rhx-size="small" rhx-href="/Docs/Components/DateTimePicker">View docs</rhx-button>
        </div>
        <div class="docs-feature-card">
            <h3>Playwright E2E in CI</h3>
            <p>The end-to-end suite is re-enabled and stabilized — Chromium runs per-PR and a full browser matrix runs nightly, gating changes on real browser regression tests.</p>
        </div>
    </div>
</section>

<rhx-divider />

<!-- What's Next -->
<section style="max-width: 900px; margin: 0 auto; padding: var(--rhx-space-2xl) var(--rhx-space-xl);">
    <h2 style="text-align: center; font-size: var(--rhx-font-size-2xl); font-weight: var(--rhx-font-weight-bold); margin-bottom: var(--rhx-space-lg); color: var(--rhx-color-text);">
        What's Next — the 2.X Roadmap
    </h2>
    <p style="text-align: center; color: var(--rhx-color-text-muted); margin-bottom: var(--rhx-space-xl);">
        Three planned minor releases, each tracked as a GitHub milestone.
    </p>

    <div class="docs-features">
        <div class="docs-feature-card">
            <rhx-badge rhx-variant="neutral">v2.2</rhx-badge>
            <h3>Playground &amp; DX</h3>
            <p>Component Playground coverage for every form and action component, plus JetBrains plugin parity with the VS Code snippets.</p>
        </div>
        <div class="docs-feature-card">
            <rhx-badge rhx-variant="neutral">v2.3</rhx-badge>
            <h3>Data &amp; Visualization</h3>
            <p>SVG bar/line/area charts, expandable and tree table rows, and an optional virtualized table body.</p>
        </div>
        <div class="docs-feature-card">
            <rhx-badge rhx-variant="neutral">v2.4</rhx-badge>
            <h3>A11y &amp; i18n</h3>
            <p>WCAG 2.2 contrast audit, RTL support, localizable strings, and clearing the infrastructure-cleanup backlog.</p>
        </div>
    </div>

    <div style="text-align: center; margin-top: var(--rhx-space-xl);">
        <rhx-button rhx-variant="brand" rhx-appearance="outlined" rhx-href="/Roadmap">
            See the full roadmap
        </rhx-button>
    </div>
</section>

<rhx-divider />

<!-- Quick Start -->
<section class="docs-quickstart">
    <h2>Quick Start</h2>

    <div class="docs-quickstart__step">
        <h3>1. Install the package</h3>
        <div class="docs-code">
            <pre><code>dotnet add package htmxRazor</code></pre>
        </div>
    </div>

    <div class="docs-quickstart__step">
        <h3>2. Configure in Program.cs</h3>
        <div class="docs-code">
            <pre><code>builder.Services.AddhtmxRazor();

var app = builder.Build();
app.UsehtmxRazor();
app.MapRazorPages();</code></pre>
        </div>
    </div>

    <div class="docs-quickstart__step">
        <h3>3. Register Tag Helpers</h3>
        <div class="docs-code">
            <pre><code>@@addTagHelper *, htmxRazor</code></pre>
        </div>
    </div>

    <div class="docs-quickstart__step">
        <h3>4. Use a component</h3>
        <div class="docs-code">
            <pre><code>&lt;rhx-button rhx-variant="brand" hx-get="/api/data" hx-target="#result"&gt;
    Load Data
&lt;/rhx-button&gt;</code></pre>
        </div>
    </div>
</section>

<rhx-divider />

<!-- Example App -->
<section style="max-width: 700px; margin: 0 auto; padding: var(--rhx-space-2xl) var(--rhx-space-xl);">
    <rhx-callout rhx-variant="brand" rhx-icon="check-circle">
        <strong>See it in action</strong> — The
        <a href="https://github.com/cwoodruff/htmxRazor/tree/main/htmxRazor.Example"
           style="color: var(--rhx-color-brand-600); text-decoration: underline;">
            htmxRazor.Example</a>
        project is a complete To-Do app built with htmxRazor. It demonstrates real-world usage
        of buttons, inputs, dialogs, tabs, badges, tooltips, and htmx-powered interactions
        — all in under 200 lines of Razor markup.
        <div style="margin-top: var(--rhx-space-md);">
            <rhx-button rhx-variant="brand" rhx-appearance="outlined" rhx-size="small"
                        rhx-href="https://github.com/cwoodruff/htmxRazor/tree/main/htmxRazor.Example"
                        rhx-target="_blank">
                <rhx-icon rhx-name="external-link" rhx-size="16" />
                View Example on GitHub
            </rhx-button>
        </div>
    </rhx-callout>
</section>

<rhx-divider />

<!-- Component Categories -->
<section style="padding: var(--rhx-space-2xl) var(--rhx-space-xl);">
    <h2 style="text-align: center; font-size: var(--rhx-font-size-2xl); font-weight: var(--rhx-font-weight-bold); margin-bottom: var(--rhx-space-xl); color: var(--rhx-color-text);">
        Component Categories
    </h2>

    <div class="docs-category-grid">
        @foreach (var cat in Model.Categories)
        {
            <a href="@cat.Href" class="docs-category-card">
                <rhx-card>
                    <div style="text-align: center; padding: var(--rhx-space-md);">
                        <rhx-icon rhx-name="@cat.Icon" rhx-size="24" />
                        <h3 style="font-size: var(--rhx-font-size-md); margin: var(--rhx-space-sm) 0 var(--rhx-space-xs);">@cat.Name</h3>
                        <rhx-badge rhx-variant="neutral">@cat.Count components</rhx-badge>
                    </div>
                </rhx-card>
            </a>
        }
    </div>
</section>
